{% extends 'AppBundle::layout.html.twig' %}

{% block menu_calendar %}active{% endblock %}

{% block contents %}
    <section>
    	<div class="container">
    	<h3>Calendrier</h3>
        {% for episode in episodes %}
            <h4>{{ episode.name }} (date : {{ episode.date|date("d/m/Y") }})</h4>
        </a>
        {% endfor %}
    	</div>
    </section>
{% endblock %}
